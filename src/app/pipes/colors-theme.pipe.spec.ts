import { ColorsThemePipe } from './colors-theme.pipe';

describe('ColorsThemePipe', () => {
  it('create an instance', () => {
    const pipe = new ColorsThemePipe();
    expect(pipe).toBeTruthy();
  });
});
